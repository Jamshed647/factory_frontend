import {
  fetchBankAccounts,
  fetchCompanies,
  fetchFactories,
  fetchProductById,
  fetchProductCategories,
} from "./useDataHooks";

const DataFetcher = {
  fetchFactories,
  fetchCompanies,
  fetchBankAccounts,
  fetchProductCategories,
  fetchProductById,
};

export default DataFetcher;
