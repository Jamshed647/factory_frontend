import {
  fetchBankAccounts,
  fetchCompanies,
  fetchFactories,
  fetchProductCategories,
} from "./useDataHooks";

const DataFetcher = {
  fetchFactories,
  fetchCompanies,
  fetchBankAccounts,
  fetchProductCategories,
};

export default DataFetcher;
