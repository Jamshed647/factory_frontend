import {
  fetchBankAccounts,
  fetchCompanies,
  fetchFactories,
} from "./useDataHooks";

const DataFetcher = {
  fetchFactories,
  fetchCompanies,
  fetchBankAccounts,
};

export default DataFetcher;
